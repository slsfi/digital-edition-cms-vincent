import { RangeArrayPipe } from './range-array.pipe';

describe('RangeArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new RangeArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
